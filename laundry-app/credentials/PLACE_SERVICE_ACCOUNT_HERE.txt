Save your Firebase private key JSON here as exactly:

  firebase-service-account.json

How to get it:
  Firebase Console → Project settings (gear) → Service accounts → Generate new private key

This is NOT google-services.json.

Then from laundry-app run:
  yarn supabase:secret:firebase
