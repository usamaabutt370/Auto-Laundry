import { PartnerServicesScreen } from "@/components/partner-services-screen";

/** Onboarding step 2: Services (same screen as Settings, title "Services"). */
export default function PartnerOnboardingStep2() {
  return <PartnerServicesScreen mode="onboarding" />;
}
