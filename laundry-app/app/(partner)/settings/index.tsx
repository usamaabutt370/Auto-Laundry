import { PartnerServicesScreen } from "@/components/partner-services-screen";

/** Settings: Merchant Services (same screen as onboarding Services, title "Merchant Services"). */
export default function SettingsIndexScreen() {
  return <PartnerServicesScreen mode="settings" />;
}
