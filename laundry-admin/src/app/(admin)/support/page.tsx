import { PageShell } from "@/features/admin/components/page-shell";

export default function SupportPage() {
  return (
    <PageShell
      title="Support"
      description="Track support tickets and respond to user issues."
    />
  );
}
